# Core dependencies (required)
Flask==3.0.0
pandas==2.1.4
numpy==1.24.3
scikit-learn==1.3.2
flask-cors==4.0.0

# Optional: TensorFlow for LSTM model (requires Python 3.8-3.11)
# If not installed, the app will use a fallback prediction method
# tensorflow==2.13.0  # Uncomment if using Python 3.8-3.11
